<% if @city.nil? %>
	
	<section class = "header">
	  <div class="content">
	    <div class="container">
	      <h1 class = 'cheers'>Oops!</h1>
	      <p>We couldn't find a city</p>
	      <h2>Here's a cat</h2>
	    </div>
	  </div>
	</section>

<% else %>

	<% provide(:title, @city.name) %>
	<section class = "header">
		<div class="content">
			<div class="container">
				<h1 class = 'cheers'><%= @city.cheers%>!</h1>
				<p>It's 5 O'Clock in</p>
				<h2><%= @city.name %></h2>
			</div>
		</div>
	</section>

	<%= render(:partial => "fun_facts/fun_fact", :collection => @fun_facts) unless @fun_facts.blank? %>

	<% if user_signed_in? && current_user.admin? %>
	<section class = "fact">
		<div class="content">
			<div class="container">
				<h3>Add fact</h3>
				<%= form_for [@city, @new_fact] do |f| %>
					<%= render '/fun_facts/fields', f: f %>
					<div class="submit">
						<%= f.submit "Add fun fact", class: "btn" %>
					</div>
				<% end %>
			</div>
		</div>
	</section>
	<% end %>
	
<% end %>

<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-43666330-1', 'cheers-project.herokuapp.com');
  ga('send', 'pageview');

</script>